git checkout -b fix commit [powstanie branch od danego commita]

git merge --squash branch
git commit
[po git merge beda wszystkie zmiany widoczne w repo a po squashu sie splaszczy i tylko ostatnia]


git reset --hard nrrewizji 
git push -f


git add -i [interaktyne dodawania do sledzenia]


git add --patch  ????
git diff --check ????
git log --no-merges
--no-merges don't print commits with more than one parent
